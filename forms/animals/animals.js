/*
let myAnimals = ["dog", "cat", "horse", "meerkat"]

for (i = 0; i < myAnimals.length; i++) {
    console.log(`${myAnimals[i]}`)
}

let addAnimals = prompt("Name an animal.")
myAnimals.push(addAnimals.toLowerCase())

console.log(`The last animal is a/an ${myAnimals[i]}.`)
*/
//only output the last animal